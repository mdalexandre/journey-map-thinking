# GitHub Actions Example

See `ci_example.yml` in this directory for a sample CI workflow that runs the
privacy grep, linters, tests, and CLI smoke test.

Copy it to `.github/workflows/ci.yml` in your own project.
